{
	"version": 2,
	"installCommand": "pnpm install --frozen-lockfile",
	"buildCommand": "pnpm build",
	"outputDirectory": ".next",
	"ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = main ]; then exit 1; fi; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- frontend/ || exit 1; exit 0"
}
