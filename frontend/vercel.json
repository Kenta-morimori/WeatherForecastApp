{
	"version": 2,
	"installCommand": "pnpm install --frozen-lockfile",
	"buildCommand": "pnpm build",
	"outputDirectory": ".next",
	"ignoreCommand": "[ \"$VERCEL_ENV\" = preview ] && exit 1; [ \"$VERCEL_GIT_COMMIT_REF\" = main ] && exit 1; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- frontend/ || exit 1; exit 0"
}
